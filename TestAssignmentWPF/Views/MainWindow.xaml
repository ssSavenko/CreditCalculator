<Window x:Class="TestAssignmentWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestAssignmentWPF.Views"
        mc:Ignorable="d"
        Title="MainWindow" MinHeight="450" MinWidth="800" MaxHeight="450" MaxWidth="800">
    <Grid Height="400" Margin="0 10 0 0" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition> 
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition> 
        </Grid.RowDefinitions>

        <UserControl Margin="5" Content="{Binding CurrentControlPanel}">
            
        </UserControl>

        <ComboBox Grid.Column="2" Height="25" VerticalAlignment="Top" Margin="3 5 7 0">
            <ComboBoxItem IsSelected="True">
                <TextBlock >Dept return encounter</TextBlock>
            </ComboBoxItem> 
        </ComboBox>
        
    </Grid>
</Window>
